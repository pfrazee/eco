exports.create = function() {
  var register = {}

  register.set = function(v) {
  }

  register.get = function() {
  }

  register.isMV = function() {
  }

  register.apply = function(msg) {
  }

  return register
}