exports.create = function() {
  var onceset = {}

  onceset.add = function(k) {
  }
  
  onceset.remove = function(k) {
  }

  onceset.has = function(k) {
  }

  onceset.apply = function(msg) {
  }

  return onceset
}